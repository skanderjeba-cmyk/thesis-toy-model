[pytest]
markers =
    fast: quick smoke tests for progression scenarios
    strict: calibration-sensitive checks (run with -m strict)
# Exclude strict tests by default (run them explicitly with: pytest -q -m strict)
addopts = -m "not strict"