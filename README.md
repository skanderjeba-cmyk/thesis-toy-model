thesis-toy-model — CLEAN SKELETON

Minimal fresh repo. Start by adding real code under:
- model/
- viz/
- scenarios/
- experiments/
- scripts/

Quick start:
  python -m pip install -r requirements.txt
  pytest
